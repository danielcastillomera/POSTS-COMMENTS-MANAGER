// IMPORTANTE: antes de hacer build de produccion, reemplaza el valor de apiUrl
// con la URL publica que Railway asigno a tu servicio de backend.
// Formato: https://<tu-servicio>.up.railway.app/api/v1
// NO debe terminar en barra diagonal (/).
export const environment = {
  production: true,
  apiUrl: 'https://posts-comments-manager-production.up.railway.app/api/v1',
};
